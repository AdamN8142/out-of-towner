import { strainsReducer } from './strainsReducer'
import * as actions from '../Actions/index'

describe('strainsReducer', () => {
 
  it('should add the strain of SATIVA', () => {
    const intialState = {
      sativa: [],
      indica: [],
      hybrid: []
    }
    
  })
})
