export const addIndica = (indica) => ({
  type: 'ADD_INDICA',
  indica
})

export const addSativa = (sativa) => ({
  type: 'ADD_SATIVA',
  sativa
})

export const addHybrid = (hybrid) => ({
  type: 'ADD_HYBRID',
  hybrid
})